# A GitHub Personal Access Token is required to fetch contribution ideas and contributor data
# from the repository at build time.
#
# Generate a token here: https://github.com/settings/tokens
# The token needs the 'public_repo' scope to read repository issues and contributors.
#
# Once generated, create a '.env' file in the root of the project and add this line:
# GITHUB_TOKEN=your_personal_access_token_here
GITHUB_TOKEN=

# Set to 'true' to disable 3D Gaussian Splat rendering during development.
# This is useful if the WebGL viewer causes performance issues or crashes.
# When disabled, a static preview image is shown instead.
DISABLE_SPLAT=