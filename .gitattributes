# Git LFS is intentionally not used: Cloudflare Workers Builds clones the repo
# without LFS credentials, and LFS bandwidth quotas break CI checkouts.
# Media assets are committed as regular blobs; large raw footage belongs in R2.
